## REST API content developer cheat sheet

Being a collection of guidance assembled throughout the migration of REST API documentation from ACOM to [docs.microsoft.com](http://docs.microsoft.com).

## Repo layout

| Directory | Contents | Editable by/contact | Notes/intended usage |
| --------- | -------- | -------------------- | -------------------- |
| api-doc   | overwrite files (*.md) | content devs | Files containing the overwrites for REST operation and parameter `summary` and `description` fields. |
| api-index | index.md | Bryan Lamos (bryanla) | all-up index (landing page) for [Azure REST API](https://review.docs.microsoft.com/en-us/rest/api/) |
| api-ref   | generated API reference (*.json) | continuous integration (generated--**do not manually edit**) | You can [link](#linking) to these, but **do not edit** them. These are generated by continous integration (CI) when a Swagger spec is modified in the [azure-rest-api-specs](https://github.com/Azure/azure-rest-api-specs) repo.
| breadcrumb | toc.yml | Brady Gaster (bradyg) | (notes/purpose TBD from Brady) |
| documentation | index.md, toc.md, ref conceptual (*.md) | content devs | Index (landing), ToC, and [ref conceptual topics](#definitions) for individual service APIs.
| readme-csi | guidance for content devs | content devs | Guides, cheat sheets, style guides for working with REST API content on docs.microsoft.com. |

## Linking

To link from [ref conceptual](#definitions) to content within the same repo (such as specific REST operations, or other ref conceptual topics), use either of the following two formats:

* `[text](../../api-ref/<service>/<file>.json#<anchor>`
* `[text](~/api-ref/<service>/<file>.json#<anchor>`

Examples:
* `[Resource group operations](../../api-ref/resources/resourcegroups.json)`
* `[Create a resource group](../../api-ref/resources/resourcegroups.json#ResourceGroups_CreateOrUpdate)`
* `[Resource group operations](~/api-ref/resources/resourcegroups.json)`
* `[Create a resource group](~/api-ref/resources/resourcegroups.json#ResourceGroups_CreateOrUpdate)`

## Metadata

Each topic that you place in the [/documentation](../documentation) directory needs a metadata section at the top. Include the the triple-hyphens (`---`) before/after the fields.

```yaml
---
ms.assetid: leave BLANK for now
ms.title: page title
ms.prod: get from list
ms.service: get from list
author: githubalias
ms.author: msalias
ms.manager: msalias
---
```

* Use only *one* of `ms.prod` or `ms.service`, do not use both.
* You can find the **get from list** list of available metadata field values at [aka.ms/skyeye/meta](https://aka.ms/skyeye/meta).

## Definitions

* **ref conceptual** -- Markdown topics that are REST API-specific for a service, but are not generated by CI ([example 1](../documentation/documentdb/restful-interactions-with-documentdb-resources.md), [example 2](../documentation/batchservice/batch-status-and-error-codes.md)).